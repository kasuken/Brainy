# Add a Playwright end-to-end test suite
<!-- labels: enhancement,priority: P2,area: platform -->

## Problem

There is no Playwright reference anywhere in the solution. `Brainy.Web.Tests` covers markup, identity, security, themes and production surfaces — valuable, but none of it drives a real browser. The ratio is 62 test files against 472 source files, and the parts with the least coverage are the newest and most stateful: offline sync, the service worker, the share target, interactive-server reconnection.

`AGENTS.md` already asks for a test *"at the boundary where the defect was observable"* for user-visible flows. For a Blazor Interactive Server app, that boundary is the browser.

## Scope

- Playwright test project wired into `Brainy.slnx` and `ci.yml`.
- Cover the primary loop: register, capture, process from Inbox, create a task, set current focus, complete, generate an output.
- Cover the flows unit tests structurally cannot reach: offline capture and reconnect sync, PWA share target, circuit reconnection.
- Deterministic seeding and teardown per test, against real SQL Server rather than EF InMemory.
- Traces and screenshots retained on failure in CI.

## Guardrails

- No AI provider calls in CI — use `AiProviderType.None` or a fake.
- Tests must be reliable. A flaky E2E suite is worse than none; quarantine is not an acceptable resting state.
- Keep runtime bounded so CI stays usable.

## Acceptance criteria

- [ ] The capture-to-output loop passes end to end in CI.
- [ ] Offline capture and subsequent sync are covered.
- [ ] Failures produce a trace and screenshot.
- [ ] The suite runs green ten consecutive times before it is considered done.

---
_Generated by [Claude Code](https://claude.ai/code)_
